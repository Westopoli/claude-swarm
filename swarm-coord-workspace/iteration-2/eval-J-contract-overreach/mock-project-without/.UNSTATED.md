# Unstated / Inferred Decisions

Items the user marked `STOP_AND_LOG`. Resolved with best-judgment defaults
and flagged here for later review. Disposition per USER_RESPONSES.md:
`accept-as-flagged`.

| # | Topic | Inference |
|---|-------|-----------|
| 1 | Slug length & charset | 7 chars from `[A-Za-z0-9]` (62^7 ≈ 3.5 trillion). |
| 2 | Collision policy | Regenerate up to 5 times; then raise `CollisionError`. |
| 3 | Lookup miss | Raise `SlugNotFoundError`; HTTP handler maps to 404. |
| 4 | Repeat shorten of same URL | No dedup; return a fresh slug every call. |
| 5 | URL validation | Accept any non-empty string; no scheme check. |

All five are explicitly **out of contract** beyond the defaults above —
the umbrella test only pins the three "done" behaviors plus the
lookup-miss exception type.
