# Unstated Assumptions (flagged inferences)

The user marked these questions STOP_AND_LOG. Defaults below were chosen by
the planner. Disposition: **accept-as-flagged** (per user response).

| # | Question | Inferred default | Risk if wrong |
|---|----------|------------------|---------------|
| 1 | CSV dialect (delimiter, quoting, header row)? | RFC 4180: `,` delimiter, `"` quote, first row = header (Python `csv.DictReader` defaults). | TSV / semicolon-delimited files mis-parse silently into single-column rows. |
| 2 | JSON output shape (array of objects vs NDJSON)? | Single JSON array, pretty-printed (`indent=2`), trailing newline. | Downstream tools expecting NDJSON would need a rewrite; large files consume more memory than streaming NDJSON. |
| 3 | Key naming (preserve casing? snake_case?)? | Preserve header verbatim, no normalization. | Headers like `"First Name"` produce keys with spaces — fine for JSON, awkward in code. |
| 4 | Error behavior (missing file, malformed row, encoding)? | Missing file -> stderr + exit 2. Malformed CSV / decode error -> stderr + exit 1. UTF-8 strict. | Strict UTF-8 will reject Latin-1 / Windows-1252 files that are common in the wild. |
