VERDICT: HALT — Bible contradiction detected.

The wave-2 spec directly contradicts the strategy doc (bible) on the core architectural constraint. Decomposition cannot proceed. The spec must be revised before any leaves are written.

================================================================================
CONFLICTING LINES
================================================================================

BIBLE (strategy.md, lines 8–18, 28–30):
  "We do NOT use Python pandas, Polars, or any other in-process DataFrame
   library for analytical compute."

  "- Aggregation: SQL `GROUP BY` with window functions where needed.
   - Time bucketing: SQL `date_trunc()` or generated time-series CTEs.
   - Rollups: SQL materialized views.
   - Joins: SQL."

  "❌ `pandas.DataFrame.groupby` — use SQL `GROUP BY`.
   ❌ `pd.read_sql(...).resample(...)` — use SQL window functions."

SPEC (wave-2.md, lines 12–19):
  "The aggregator will use **pandas DataFrames** for the grouping and
   time-bucket logic. Specifically:

   - `pd.read_sql(...)` to pull raw events from the warehouse into a DataFrame.
   - `df.groupby([dimension, pd.Grouper(freq='1h')]).agg({'value': 'sum'})` for the rollup.
   - `df.resample('1d').mean()` for daily averages.
   - Return the resulting DataFrame as a list of ResultRow.

   The pandas approach is preferred because it gives more flexible windowing
   semantics than what's available in the warehouse SQL dialect..."

================================================================================
ANALYSIS
================================================================================

The spec's stated rationale ("more flexible windowing semantics than what's
available in the warehouse SQL dialect") is explicitly preempted by the bible,
which declares: "If you find yourself reasoning about how to implement
aggregation in Python, stop and re-read this section." The bible also forbids
loading data into memory for Python-side processing as a permanent rule, not a
preference.

The bible's authority chain (strategy.md line 33) states:
  "This doc > specs > tests > implementation."

Therefore: the spec is wrong, not the bible.

================================================================================
REQUIRED ACTION BEFORE PROCEEDING
================================================================================

The wave-2 spec must be revised to:
1. Remove all pandas/DataFrame references.
2. Restate the rollup logic as SQL (GROUP BY + window functions + date_trunc).
3. Limit Python's role to marshaling pre-aggregated results from the warehouse.
4. Update the module layout — `pandas_engine.py` is an anti-pattern name under
   the corrected approach.

Do NOT spawn any leaf agents against the current wave-2.md. Do NOT write
briefs. Return the spec to its author for revision.
