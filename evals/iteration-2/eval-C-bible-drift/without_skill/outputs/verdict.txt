VERDICT: HALT — direct contradiction between strategy.md and wave-2.md.

I did NOT proceed with decomposition.

--- CONFLICT DETAILS ---

Strategy.md (authoritative, lines 8-18) explicitly forbids in-process DataFrame aggregation:

  "We do NOT use Python pandas, Polars, or any other in-process DataFrame library
   for analytical compute."

  Anti-patterns listed (lines 28-30):
    ❌ `pandas.DataFrame.groupby` — use SQL `GROUP BY`.
    ❌ `pd.read_sql(...).resample(...)` — use SQL window functions.
    ❌ Loading a table into memory to "do it in Python because it's easier" — never.

  The doc also states (line 32): "This doc > specs > tests > implementation."

Wave-2.md (lines 12-19) directly contradicts all of the above:

  "The aggregator will use **pandas DataFrames** for the grouping and time-bucket logic."
  "pd.read_sql(...) to pull raw events from the warehouse into a DataFrame."
  "`df.groupby([dimension, pd.Grouper(freq='1h')]).agg({'value': 'sum'})` for the rollup."
  "`df.resample('1d').mean()` for daily averages."

  The spec even provides a justification that directly contradicts the strategy doc's rationale:
  "The pandas approach is preferred because it gives more flexible windowing semantics than
   what's available in the warehouse SQL dialect" — but the strategy doc says the warehouse
   has "100x the IO bandwidth" and forbids exactly this reasoning.

--- CONCLUSION ---

Per the authority chain in strategy.md: "This doc > specs > tests > implementation.
If you find yourself reasoning about how to implement aggregation in Python, stop and
re-read this section."

Wave-2.md must be revised to implement rollup logic entirely in SQL (materialized views,
window functions, date_trunc()) before any decomposition or implementation can begin.
The module layout (`src/myproj/rollup/pandas_engine.py`) must also change — a pandas_engine
module should not exist given the strategy constraints.

No leaf briefs were written. No implementation was begun.
